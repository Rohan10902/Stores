name: Legacy workflow disabled

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Store Data Assistant workflow disabled. Clean StoreLens branch is being rebuilt."
